# Orchestration Log: Dallas (UI Dev)

**Agent:** Dallas  
**Date:** 2026-06-02T10:23:37Z  
**Phase:** 1 — App Shell, System Tray & Settings  
**Mode:** Background (non-blocking)

## Why Chosen

Dallas was selected as UI Dev to implement app shell, system tray integration, and settings persistence. Phase 1 runs in parallel with Phase 2 (Parker—backend) and Phase 3 (Lambert—test data).

## Responsibilities

- Remove StartupUri from App.xaml, set ShutdownMode=OnExplicitShutdown
- Implement TrayIconService (system tray integration via WinForms NotifyIcon)
- Implement SettingsService (Load, Save to %AppData%\ElBruno.PresenterTimer\settings.json)
- Create AppSettings class with default values (GeneralSettings, BehaviorSettings, OverlayStyleSettings, OverlayLayoutSettings, AlertSettings, HotkeySettings)
- Create tray icon context menu with handlers for: Start/Pause, Stop, Resume, Load Session, Settings, About, Exit
- Implement icon state rendering (6 programmatic 16×16 Bitmap states: Gray, RoyalBlue, SeaGreen, Goldenrod, Crimson, SlateBlue)
- Add UseWindowsForms=true to csproj for NotifyIcon support
- Wire App.OnStartup and App.OnExit for graceful initialization and cleanup
- Create menu handler stubs with // TODO Phase N comments for later wiring

## Outcome

✅ Success: Tray icon visible, settings file created on first run, clean exit behavior verified.

App shell and tray integration complete. Ready for Phase 4 (Lambert—ViewModel wiring) and Phase 5+ (timer/overlay implementation).
